// Read-only version of the case detail view - reuses the same API calls
// as LegalAidDashboard but renders without action buttons.
export default function JudgeDashboard({ token }) {
  return <div><h1>Judge dashboard (read-only reference view)</h1></div>;
}
